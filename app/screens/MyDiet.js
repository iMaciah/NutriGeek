import React, { Component } from 'react';
import {
  Text,
  View,
  ScrollView
} from 'react-native';
import { List, ListItem } from 'react-native-elements';

class MyDiet extends Component {

  render() {
    return (
      <ScrollView>
        <Text>MyDiet go here</Text>
      </ScrollView>
    );
  }
}

export default MyDiet;
